# Netlify build config for Opilex Admin Panel
[build]
  command = "npm run build"
  publish = "dist"

# SPA: redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
